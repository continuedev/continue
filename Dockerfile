# Generated by https://smithery.ai. See: https://smithery.ai/docs/config#dockerfile
# Use a Node.js image
FROM node:16-alpine AS builder

# Set the working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Copy the rest of the application
COPY . .

# Build the binary
RUN node binary/build.js

# Use a smaller image for the final output
FROM node:16-alpine

# Set the working directory
WORKDIR /app

# Copy the built binaries from the builder stage
COPY --from=builder /app/binary/bin /app/bin

# Set the entrypoint to run the binary (replace `continue-binary` with the actual binary name if different)
ENTRYPOINT ["/app/bin/your-target-platform/continue-binary"]