name: Test Invokable Rules
version: 0.0.1
schema: v1

rules:
  - name: "Code Review"
    rule: "Review this code for best practices and potential issues"
    invokable: true
    description: "Performs a code review"
    
  - name: "Explain Code"
    rule: "Explain what this code does in simple terms"
    invokable: true
    
  - name: "Regular Rule"
    rule: "This is a non-invokable rule"
    invokable: false
    
  - name: "Default Rule"
    rule: "This rule has no invokable property"
    description: "Should default to non-invokable"
    
  - "Simple string rule"