---
title: xAI
slug: ../xai
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

:::info
You can get an API key from the [xAI console](https://console.x.ai/)
:::

## Chat model

We recommend configuring **grok-beta** as your chat model.

<Tabs groupId="config-example">
	<TabItem value="yaml" label="YAML">
  ```yaml title="config.yaml"
  models:
    - name: Grok Beta
      provider: xAI
      model: grok-beta
      apiKey: ${{ secrets.GROK_API_KEY }}
  ```
  </TabItem>
	<TabItem value="json" label="JSON">
  ```json title="config.json""
      "models": [
        {
          "title": "Grok Beta",
          "provider": "xAI",
          "model": "grok-beta",
          "apiKey": "<API_KEY>"
        }
      ]
    ```
  </TabItem>
</Tabs>

## Autocomplete model

xAI currently does not offer any autocomplete models.

[Click here](../../model-types/autocomplete.md) to see a list of autocomplete model providers.

## Embeddings model

xAI currently does not offer any embeddings models.

[Click here](../../model-types/embeddings.mdx) to see a list of embeddings model providers.

## Reranking model

xAI currently does not offer any reranking models.

[Click here](../../model-types/reranking.mdx) to see a list of reranking model providers.
