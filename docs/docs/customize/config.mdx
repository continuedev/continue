---
title: config.json Reference
keywords: [config.json, configuration, options]
---

import Schema from "@site/static/schemas/config.json";
import JSONSchemaViewer from "@theme/JSONSchemaViewer";

# Configuration options

The following shows the JSON Schema definition for `config.json`.

You can also define a partial `config.json` object in a file called `.continuerc.json` at the root of your workspace. This will be applied on top of any configuration from `config.json`.

<JSONSchemaViewer
  schema={Schema}
  resolverOptions={{
    jsonPointer: "#/definitions/SerializedContinueConfig",
  }}
/>

[View the source](https://github.com/continuedev/continue/blob/main/extensions/vscode/config_schema.json)
