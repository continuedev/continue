# Project-wide pnpm configuration
# Note: The lockfile will still be generated but is excluded from git via .gitignore

# Set default package manager
engine-strict=true

# General settings
resolution-mode=highest
